/*
 * bingo.h
 *
 *  Created on: 20 Nov 2022
 *      Author: kristoferrosquist
 */

#ifndef BINGO_H_
#define BINGO_H_

//struct Point;  // forward declared for encapsulation
//struct Point* Point__create(int x, int y);  // equivalent to "new Point(x, y)"
//void Point__destroy(struct Point* self);  // equivalent to "delete point"
//int Point__x(struct Point* self);  // equivalent to "point->x()"
//int Point__y(struct Point* self);  // equivalent to "point->y()"

#endif /* BINGO_H_ */
